# IMPORTANT: Change the JWT_SECRET value before deploying to production!
# Generate a new secret: echo -n "your-secret-here" | base64
apiVersion: v1
kind: Secret
metadata:
  name: deadchat-secret
  namespace: deadchat
  labels:
    app: deadchat
type: Opaque
data:
  # Base64 encoded value of "changeme-generate-a-real-secret"
  JWT_SECRET: Y2hhbmdlbWUtZ2VuZXJhdGUtYS1yZWFsLXNlY3JldA==
