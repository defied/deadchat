apiVersion: v1
kind: Service
metadata:
  name: deadchat-frontend
  namespace: deadchat
  labels:
    app: deadchat
    component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: deadchat
    component: frontend
