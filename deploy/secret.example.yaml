# Managed out-of-band; NOT synced by ArgoCD (this repo is public).
# To (re)create the live Secret:
#   JWT=$(openssl rand -hex 32)
#   kubectl -n deadchat create secret generic deadchat-secret \
#     --from-literal=JWT_SECRET="$JWT" \
#     --dry-run=client -o yaml | kubectl apply -f -
#   kubectl -n deadchat annotate secret deadchat-secret \
#     argocd.argoproj.io/sync-options=Prune=false --overwrite
apiVersion: v1
kind: Secret
metadata:
  name: deadchat-secret
  namespace: deadchat
  annotations:
    argocd.argoproj.io/sync-options: Prune=false
type: Opaque
stringData:
  JWT_SECRET: REPLACE_ME
